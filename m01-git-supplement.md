# M01 補足資料：変更の確認、ファイルの除外、コミットのやり直し

この資料は、授業では扱わない発展内容をまとめたものです。読まなくても2日目以降の内容には支障ありません。
余裕があるときに読んでください。

授業で扱ったのは、`git status`、`git add`、`git commit`、`git log` の4つでした。ここではその先の
「変更を確認する」「入れたくないファイルを除外する」「やってしまった操作を元に戻す」を扱います。

スライドから案内されている「Git入門のまとめ」ビデオは英語です。そのビデオの後半で扱われる内容を、
この資料が日本語で置き換えます。

演習1（GitHub Skills）の Step 4「Comparing Changes」は、この資料の1章と同じ内容です。授業で Step 3 まで進んだ人は、
Step 4 の代わりにこの資料を読んでも構いません。

**実行環境**: 9章のコマンドは、演習1で作った Codespace のターミナルで `/workspaces` 直下に移動してから実行できます
（`cd /workspaces` → 以下の手順）。手元の PC に Git が入っていればそこでも同じです。

## 目次

1. `git diff`：何が変わったかを見る
2. まとめて記録する：`git add -A` と `git commit -am`
3. `.gitignore`：追跡しないファイルを決める
4. `git commit --amend`：直前のコミットをやり直す
5. 消してしまったファイルを元に戻す
6. `git reset`：コミットを取り消す
7. `git revert`：取り消すためのコミットを作る
8. 場面別の早見表
9. 手を動かして確かめる

---

## 1. `git diff`：何が変わったかを見る

`git status` は「どのファイルが変わったか」を教えてくれますが、「中身がどう変わったか」までは分かりません。
それを見るのが `git diff` です。コミットする前に、自分が何を入れようとしているかを確認できます。

```bash
git diff
```

ここで大事なのは、**`git add` すると `git diff` には出てこなくなる**ことです。消えたわけではなく、
ステージに移ったためです。ステージした内容を見るには `--staged` を付けます。

```bash
git diff            # まだ add していない変更
git diff --staged   # add 済みで、次のコミットに入る変更
```

`git add` したあとに `git diff` が空になって焦ることがありますが、`--staged` を見れば残っています。

---

## 2. まとめて記録する：`git add -A` と `git commit -am`

ファイルを1つずつ `git add` するのは、数が増えると大変です。まとめて指定する書き方があります。

```bash
git add -A          # リポジトリ全体の変更（追加・変更・削除）をすべてステージ
git add .           # 今いるディレクトリ以下の変更をステージ
```

`git add .` も削除を拾います。「今いるディレクトリより下だけ」か「リポジトリ全体か」が違いです。

コミットまで一度に済ませる書き方もあります。

```bash
git commit -am "メッセージ"
```

### `-a` は新しいファイルを含みません

`-a` は「追跡中のファイルの変更」をステージします。**まだ一度も `git add` していない新しいファイルは含まれません。**

新しいファイルを作った直後に `git commit -am` を実行すると、そのファイルだけコミットから漏れます。
実行後に `git status` を見て、`??` が残っていないか確認する癖を付けてください。

---

## 3. `.gitignore`：追跡しないファイルを決める

ビルド結果、ログ、エディターの設定、ローカル環境だけで使う値など、リポジトリに入れたくないファイルがあります。
リポジトリの直下に `.gitignore` という名前のファイルを作り、除外したいものを1行ずつ書きます。

```text
# 行頭の # はコメントです
node_modules/
*.log
build/
.env
!important.log
```

書き方の意味は次のとおりです。

| 書き方 | 意味 |
|--------|------|
| `node_modules/` | 末尾の `/` はディレクトリを指す。中身ごと除外する |
| `*.log` | `*` は任意の文字列。拡張子でまとめて除外する |
| `build/` | ビルド結果のディレクトリ |
| `.env` | ファイル名をそのまま書く |
| `!important.log` | `!` を付けた行は除外の解除。`*.log` で除外したうちこれだけ残す |

上の例をコピーする場合、説明のほうは書き写さないでください。`.gitignore` は1行がそのままパターンとして
読まれるため、行の後ろに説明を書くと、その説明を含んだ名前のファイルを探しにいってしまいます。

`git status` に出てこなくなれば効いています。どのルールが効いたのか確かめたいときは次を実行します。

```bash
git check-ignore -v .env
```

### 効くのは「まだ追跡していないファイル」だけです

ここが間違えやすいところです。**一度コミットしたファイルは、後から `.gitignore` に書いても除外されません。**
Gitはすでに追跡を始めているためです。

追跡を外したい場合は、作業ツリーのファイルは残したまま、Gitの管理下からだけ外します。

```bash
git rm --cached .env
git commit -m ".env を追跡対象から外す"
```

### 機密情報の対策としては不十分です

`.gitignore` は「うっかり追加しない」ための仕組みであって、漏れてしまったものを取り消す仕組みではありません。
パスワードやトークンを一度コミットしてしまうと、その後 `.gitignore` に書いても、`git rm --cached` しても、
**過去のコミットには残り続けます。** すでにcloneされていれば相手の手元にも残ります。

この場合にやるべきことは、まず**その資格情報を無効化して作り直すこと**です。履歴の掃除はその後の話になります。
モジュール11でこの話を扱いますので、そこで詳しく確認してください。

### `.gitkeep`：空のディレクトリを残す

除外の裏返しの話です。**Gitはファイルを記録する仕組みなので、空のディレクトリは記録できません。**
`mkdir logs` としても、`git add -A` で何も検出されません。

ディレクトリの構造だけ共有したいときは、慣習として中身のない `.gitkeep` を置きます。

```bash
mkdir logs
touch logs/.gitkeep
git add -A
```

`.gitkeep` はGitの機能ではなく、単なる名前の約束です。中身は空で構いません。

参考：[ファイルを無視する（GitHub Docs）](https://docs.github.com/ja/get-started/git-basics/ignoring-files)

---

## 4. `git commit --amend`：直前のコミットをやり直す

コミットした直後に、メッセージの間違いや、`git add` し忘れたファイルに気づくことがあります。
新しいコミットを重ねずに、直前のコミットを作り直せます。

```bash
# メッセージだけ直す
git commit --amend -m "正しいメッセージ"

# 入れ忘れたファイルを足す（メッセージはそのまま）
git add 忘れていたファイル
git commit --amend --no-edit
```

### push した後には使わないでください

`--amend` は、直前のコミットを**別のコミットに置き換えます。** コミットを識別するIDが変わるため、
すでにpushしてある場合、リモートの履歴と自分の履歴が食い違います。強制pushで上書きすることになり、
同じリポジトリで作業している人の手元が壊れます。

**push する前だけの操作**と覚えてください。push 済みの場合にどうするかは、8章の早見表にまとめています。

---

## 5. 消してしまったファイルを元に戻す

状況によって戻し方が違います。まず `git status` で今どの状態かを確認してください。

### 消しただけで、まだ何もしていない

```bash
git restore ファイル名
```

### `git rm` した、または削除を `git add` してしまった

ステージから戻してから、中身を戻します。

```bash
git restore --staged --worktree ファイル名
```

### 過去のコミットから取り出す

そのファイルを触ったコミットを探してから、そこから取り出します。ファイルは複数まとめて指定できます。

```bash
git log --oneline -- ファイル名
git restore --source=コミットID ファイル名 別のファイル名
```

取り出した内容は作業ツリーに置かれるだけで、コミットはされません。`git status` で変更として見えるので、
確認してから `git add` と `git commit` を行ってください。

`git restore` はGit 2.23以降のコマンドです。古い環境では `git checkout -- ファイル名` を使います。
`git checkout` はブランチの切り替えとファイルの復元という別々の役割を兼ねていて紛らわしいため、
現在は `restore`（ファイル）と `switch`（ブランチ）に分かれています。

---

## 6. `git reset`：コミットを取り消す

コミットそのものをなかったことにします。どこまで戻すかを3種類から選びます。

| 指定 | コミット | ステージ | 作業ツリー（編集内容） |
|------|---------|---------|----------------------|
| `--soft` | 取り消す | 残る | 残る |
| `--mixed`（既定） | 取り消す | 取り消す | 残る |
| `--hard` | 取り消す | 取り消す | **消える** |

```bash
# 直前のコミットを取り消して、変更はステージに残す
git reset --soft HEAD~1

# 直前のコミットとステージを取り消して、編集内容は手元に残す
git reset HEAD~1
```

### `--hard` で失われるものと、失われないもの

`--hard` は危険だとよく言われますが、2つを分けて理解しておくと落ち着いて扱えます。

- **すでにコミットしてあった内容**は、`git reflog` に移動の記録が残るので、戻せる場合があります
- **まだコミットしていない編集**は失われます。こちらは戻す手段がありません

```bash
git reflog                    # 直前までの HEAD の移動履歴を見る
git reset --hard コミットID    # reflog で見つけたIDへ戻す
```

つまり本当に危ないのは「作業中でまだコミットしていないもの」です。不安なときは、先にコミットしておくか、
`git stash` で退避してから操作してください。

`reset` も履歴を書き換えます。`--amend` と同じく、**push する前だけ**の操作です。

---

## 7. `git revert`：取り消すためのコミットを作る

すでにpushしたコミットの変更を取り消したいときはこちらです。履歴を書き換えるのではなく、
**その変更を打ち消す新しいコミットを追加します。**

```bash
git revert コミットID
```

実行するとコミットメッセージの編集画面が開きます。既定のメッセージのままでよければ、
`--no-edit` を付けると開かずに済みます。

```bash
git revert --no-edit HEAD
```

元のコミットは履歴に残り、その後ろに「打ち消した」というコミットが並びます。他の人の履歴と食い違わないため、
共有済みのリポジトリではこちらが基本です。

「間違いを消す」のではなく「間違いを直した記録を残す」やり方だと考えてください。

### `revert` は変更内容を打ち消す操作です

コミットメッセージの誤字を直す用途には使えません。`revert` が打ち消すのはファイルの中身であって、
メッセージではないためです。メッセージの誤字をどうするかは、次章の早見表を見てください。

参考：[git-revert（公式ドキュメント）](https://git-scm.com/docs/git-revert)

---

## 8. 場面別の早見表

やり直しの方法は「何を直したいか」と「pushしたかどうか」の組み合わせで決まります。

| 直したいこと | pushしていない | push済み |
|--------------|---------------|---------|
| ファイルを入れ忘れた | `git commit --amend` | **新しいコミットで追加する** |
| コミットメッセージの誤字 | `git commit --amend` | **原則そのままにする**（直すと履歴の書き換えになるため、チームの方針に従う） |
| 変更内容そのものを取り消す | `git reset` | `git revert` |
| コミットしていない編集を捨てる | `git restore` | — |

判断の軸は一つです。**他の人がその履歴をすでに見ているかどうか。** 見られていなければ書き換えてよく、
見られていれば打ち消しを足すか、そのままにします。

メッセージの誤字は、直すために履歴を書き換えると、他の人の手元と食い違います。誤字の実害と、
全員に影響する書き換えの手間を比べて判断してください。多くのチームでは、そのままにします。

迷ったときは、まず `git status` を実行してください。今どの状態にいるのかが分かれば、選ぶコマンドも決まります。

---

## 9. 手を動かして確かめる

**授業で作った演習用リポジトリは使わないでください。** あのリポジトリは後半のモジュールでも使うため、
ここでの操作で履歴を変えてしまうと支障が出ます。使い捨てのリポジトリを新しく作って試します。

上から順にコピーして実行できます。途中で止めても構いません。

```bash
# 0. 使い捨てのリポジトリを作る
mkdir git-practice
cd git-practice
git init -b main
git config user.name "あなたの名前"
git config user.email "あなたのメールアドレス"

# 1. git diff の位置を確かめる
echo "1行目" > memo.txt
git add memo.txt
git commit -m "memo.txt を追加"
echo "2行目" >> memo.txt
git diff                  # 変更が見える
git add memo.txt
git diff                  # 空になる
git diff --staged         # こちらに移っている
git commit -m "2行目を追加"

# 2. commit -am は新しいファイルを拾わない
echo "追記" >> memo.txt
echo "新しいファイル" > new.txt
git commit -am "memo を更新"
git status                # new.txt が ?? として残る

# 3. .gitignore は追跡済みに効かない
git add -A
git commit -m "new.txt を追加"
echo "new.txt" > .gitignore
git add .gitignore
git commit -m ".gitignore を追加"
echo "さらに追記" >> new.txt
git status                # new.txt が出る = 除外されていない
git rm --cached new.txt
git commit -m "new.txt の追跡をやめる"
git status                # 何も出なくなる

# 4. 直前のコミットをやり直す
git log --oneline -1
git commit --amend -m "メッセージを書き直した"
git log --oneline -1      # IDもメッセージも変わっている

# 5. 消したファイルを戻す
rm memo.txt
git restore memo.txt
cat memo.txt

# 6. revert は打ち消しコミットを足す
echo "消す予定" > temp.txt
git add temp.txt
git commit -m "temp.txt を追加"
git revert --no-edit HEAD
git log --oneline         # 元のコミットが残り、Revert が足される
ls                        # temp.txt は消えている

# 7. reset --hard と reflog（使い捨てリポジトリなので試せます）
git log --oneline -1
git reset --hard HEAD~1   # 直前のコミットを捨てる
git log --oneline -1      # 1つ前になっている
git reflog                # 捨てたコミットのIDが記録に残っている
```

最後の `git reflog` に、直前まで指していたコミットのIDが出ます。そのIDを使って
`git reset --hard そのID` を実行すると、捨てたコミットへ戻れます。試してみてください。

確認が終わったら、`git-practice` ディレクトリは削除して構いません。

---

## さらに学ぶ

- [Git の概要（Microsoft Learn・日本語）](https://learn.microsoft.com/ja-jp/training/modules/intro-to-git/)
- [Git Book 日本語版](https://git-scm.com/book/ja/v2)
- [ファイルを無視する（GitHub Docs）](https://docs.github.com/ja/get-started/git-basics/ignoring-files)
- [git-reset（公式ドキュメント）](https://git-scm.com/docs/git-reset)
- [git-reflog（公式ドキュメント）](https://git-scm.com/docs/git-reflog)
